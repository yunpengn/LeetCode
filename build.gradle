apply plugin: 'java'

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
    test {
        useJUnitPlatform()
        java {
            srcDirs = ['test']
        }
    }

    task wrapper(type: Wrapper) {
        gradleVersion = '4.6'
    }
}
